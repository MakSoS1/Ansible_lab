# MakSoS1 / Ansible_lab — AIOS Track 2

Ветка `aios-track2-v2`, PR https://github.com/MakSoS1/Ansible_lab/pull/8

## Что сдаём

- `submission/wells_schedule.inc` — расписание победителя MAPPO, независимо пересчитанное OPM Flow 2026.04 + ЧДД 7.0.2
- NPV **12 475.954558553085 млн ₽** (baseline 11 891.994046426242)
- max WLPR **62.55 ≤ 500**
- SHA `c5ff3221ac66dea460bbd638a589dc5c7f2dedeb1536b9f86b10fb2e3e030af3`
- Hugging Face: `Maksim123321/aios-track2-runs` / `runs/9ad40738134d-33925326455/final-mappo`

## Одна команда

```bash
python -m pip install -e '.[dev,api]'
python -m aios_track2.cli ui --submission submission
```

UI: http://127.0.0.1:8765. Интерфейс без сборки — ни npm, ни node не нужны, страница отдаётся
FastAPI как статика, поэтому запускается в любой среде проверки и работает офлайн.

Docker:

```bash
docker build -t aios-track2 .
docker run --rm -p 8765:8765 aios-track2
```

Тесты: `PYTHONPATH=src pytest -q`

## Интерфейс

Три экрана, ровно под то, что нужно проверяющему.

**Прогон.** Запускает систему и показывает журнал из девяти агентов: Monitor, Reservoir
Diagnostic, Planning, Constraint Guard, Surrogate, Simulator, Economics, Reproducibility,
Explanation. Каждый шаг делает настоящую проверку и возвращает своё основание, а не текст из
шаблона. Два режима:

- `Проверить сабмит` — прогоняет весь контур приёмки по артефактам `submission/` за ~1 с;
- `Пересобрать из деки` — заново строит `wells_schedule.inc` из нетронутого
  `41_Model_Z_final_OPM.zip` и вектора политики, затем сверяет SHA‑256 с заявленным
  (~6 с). Это живое доказательство воспроизводимости по п. 3.5 и 4.4 ТЗ.

Скачивание расписания разблокируется только когда прогон дошёл до `VERIFIED`: если хотя бы
один агент вернул `fail`, выпуск заблокирован.

**Результат.** Помесячная добыча, действующий фонд и средний отбор жидкости — базовое
расписание против решения системы. Все 75 смен насосов, 23 остановки и пуска и 9 переводов
под закачку с их стоимостью. Приёмочные пороги суррогата, включая непройденный top‑3.

**Промысел.** 103 скважины в координатах сетки: роль, группа управления, назначенный режим,
сколько месяцев скважина работала в каждой роли.

Никакие числа в интерфейсе не зашиты в вёрстку: страница рисует то, что вернул
`aios_track2.run_service`, а тот считает по `submission/` и по исходной деке.

### API

| Метод | Путь | Назначение |
|---|---|---|
| GET | `/api/health` | статус артефактов и доступность архива Model Z |
| GET | `/api/case` | идентификация кейса: сетка, фонд, период, SHA деки |
| GET | `/api/metrics` | ЧДД, Δ к базе, максимальный WLPR, число расчётов OPM |
| GET | `/api/field` | 103 скважины: роль, группа, назначенный режим |
| GET | `/api/production` | помесячные ряды, базовое расписание и решение |
| GET | `/api/annual` | экономика по годам |
| GET | `/api/operations` | смены насосов, остановки, переводы под закачку |
| GET | `/api/quality` | пороги суррогата и паритет с эталоном ЧДД |
| GET | `/api/explanation` | политика по группам и предупреждения системы о себе |
| POST | `/api/runs` | запустить прогон: `{"mode": "verify"}` или `{"mode": "rebuild"}` |
| GET | `/api/runs/{id}` | состояние прогона и журнал агентов |
| GET | `/api/runs/{id}/schedule` | `wells_schedule.inc` — только для `VERIFIED` |
| GET | `/api/runs/{id}/report.json` | полный отчёт прогона |

## Честные ограничения

- Сданное управление: **18D** (4 группы добычи + 2 группы закачки × узлы 2007/2016/2025), масштабирование существующих WCON. Это не полный поскважинный open/close / циклика / перевод.
- Код перевода, останова и циклики есть (`aios_track2.well_actions`) и покрыт тестами, но **не входит в сданное расписание**: нет отдельного OPM-подтверждения этих действий.
- Preregistered surrogate gate **не пройден**: top-3 recall = 2/3. Турнир финалистов всё равно авторизован по Spearman/pairwise/simple regret, а победитель выбран по OPM NPV.
- Хвост суррогата: P10 scenario×channel R² ≈ 0.80; худший канал FOPT сценария 29 имеет R² ≈ −16.17. Не прячем.
- PINCHREG: прогоны OPM с `--parsing-strictness=low`. Организатор на strict-парсере может получить другой NPV.

Ветки `ecup*` к этому треку не относятся.
