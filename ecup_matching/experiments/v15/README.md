# v15 handoff

Canonical v15 experiment contract lives in `PLAN.md`; machine-safe aggregates in `SAFE_METRICS.json`; immutable identifiers in `MANIFEST.json`; retained evidence in `RESULTS.md`.

Do not use this directory to record private credentials, raw datasets, private OOF files, models or submission archives.
